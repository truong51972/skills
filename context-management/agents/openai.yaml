interface:
  display_name: "Context Management"
  short_description: "Autonomously maintain durable repository context"
  default_prompt: >
    Manage repository context autonomously throughout this task. Load only
    relevant context, verify important context facts against source files,
    detect drift, and synchronize durable changes when needed.
